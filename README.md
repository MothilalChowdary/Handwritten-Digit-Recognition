# Handwritten-Digit-Recognition
This project recognizes handwritten digits (0-9) using Convolutional Neural Networks (CNNs). We train a model using the MNIST dataset, which contains 60,000 training images and 10,000 testing images of handwritten digits.
